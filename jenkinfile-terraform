pipeline {
  agent any

  environment {
    AWS_REGION = "us-east-1"
    KUBECONFIG = "${WORKSPACE}/.kube/config"
  }

  stages {
    stage('Init Terraform') {
      steps {
        dir('eks_terraform') {
          sh 'terraform init'
        }
      }
    }

    stage('Plan Terraform') {
      steps {
        dir('eks_terraform') {
          sh 'terraform plan'
        }
      }
    }

    stage('Apply Terraform') {
      steps {
        dir('eks_terraform') {
          sh 'terraform apply -auto-approve'
        }
      }
    }

    stage('Wait for EKS cluster readiness') {
      steps {
        retry(5) {
          sh '''
            echo "⏳ Waiting for EKS to be ACTIVE..."
            sleep 20
            aws eks describe-cluster --region ${AWS_REGION} --name EKS_CLOUD --query 'cluster.status' --output text | grep ACTIVE
          '''
        }
      }
    }

    stage('Update kubeconfig') {
      steps {
        sh '''
          echo "🔁 Updating kubeconfig to connect to EKS..."
          mkdir -p $(dirname ${KUBECONFIG})
          aws eks update-kubeconfig --region ${AWS_REGION} --name EKS_CLOUD --kubeconfig ${KUBECONFIG}
        '''
      }
    }

    stage('Configure aws-auth ConfigMap') {
      steps {
        sh '''
          echo "🔧 Patching aws-auth config map..."
          # Example (you'll need to modify this based on IAM role setup)
          # kubectl apply -f aws-auth-patch.yaml
        '''
      }
    }

    stage('Verify Access') {
      steps {
        sh '''
          echo "🔍 Verifying access to cluster"
          kubectl get nodes
        '''
      }
    }
  }

  post {
    failure {
      echo "❌ Pipeline failed. Please check logs."
    }
  }
}
